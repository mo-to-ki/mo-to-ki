# github-command
目次
> [git config](#config)  
    configファイルの編集

<a id=config></a>
## git config

`github`の`config`ファイルを設定するためのコマンド  
このコマンドでは、`viエディタ`でローカルの`config`ファイルを設定することができる。
```shell
git config --local -e
```
