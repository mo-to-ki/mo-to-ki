# github-command
目次
> [git config](#config)  
    configファイルの編集

<a id=config></a>
## git config
`github`の`config`ファイルを設定するためのコマンド
```shell
git config --local -e
```
このコマンドでは、`viエディタ`で`config`の設定を行うことができる。